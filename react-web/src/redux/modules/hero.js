// action types
const types = {

};

// 异步actions
const actions = {
  
}

// 同步action creator

// 初始状态
const initialState = {

};

// reducer
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default reducer;

// selectors
